services:

  ib-gateway:
    image: ghcr.io/gnzsnz/ib-gateway:stable
    restart: unless-stopped
    environment:
      TWS_USERID: ${IBKR_USERNAME}
      TWS_PASSWORD: ${IBKR_PASSWORD}
      TRADING_MODE: ${TRADING_MODE:-paper}
      JAVA_HEAP_SIZE: 512
      EXISTING_SESSION_DETECTED_ACTION: primary
      READ_ONLY_API: no
    ports:
      - "4004:4004"   # socat relay → paper API (bypasses TrustedIPs)
      - "4003:4003"   # socat relay → live API
    volumes:
      - ib-gateway-settings:/home/ibgateway

  smc-trader:
    build: .
    restart: unless-stopped
    depends_on:
      - ib-gateway
    environment:
      IBKR_HOST: ib-gateway
      IBKR_PORT: ${IBKR_PORT:-4004}
      IBKR_CLIENT_ID: ${IBKR_CLIENT_ID:-1}
      ACCOUNT_SIZE: ${ACCOUNT_SIZE:-10000}
      MASSIVE_API_KEY: ${MASSIVE_API_KEY:-}
      TELEGRAM_TOKEN: ${TELEGRAM_TOKEN:-}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID:-}
      USE_FULL_SP500: ${USE_FULL_SP500:-false}
    volumes:
      - trader-data:/app/data
      - trader-logs:/app/logs

volumes:
  ib-gateway-settings:
  trader-data:
  trader-logs:
